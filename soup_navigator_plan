objects:

(the important one)
1. tag:
- .name -> tag name
- .string -> NavigableString (can convert to unicode with unicode(str))
- ["attribute"] -> (list if multi value attribute)
- .attrs -> (attributes directory)
- .get_attribute_list(<attribute>) -> always returns a list of values

2. BeautifulSoup:
- can just treat it as a tag

navigation:

1. find_all(name, attrs, recursive, string, limit, **kwargs):
- name: looks for tag name, ignores everything else
- attrs: dict[attribute: value]
- recursive: True for searching recursively, False otherwise. True by default.
- string: looks for strings after filtering through names and attrs
- limit: limit the number of items in the list
- kwargs: non-recognized arguments will be applied as an attribute filter
- always returns list

2. find:
- same as find_all except only returns the first result

idea:

{
  "__nav__": Literal["function", "method", "property"],
  "data": BeautifulSoup | Tag | Nav | Reference,
  "name": "", # one of function / method or property
  "kwargs": dict[str, Any], # for function and method
  "select": int | list[int] # for when you wanna return specific items from a resulted list
}

Reference: {"__ref__": str}
